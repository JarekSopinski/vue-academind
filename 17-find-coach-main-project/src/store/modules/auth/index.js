import mutation from './mutations';
import actions from './actions';
import getters from './getters';

export default {
    state() {
        return {
            userId: 'c3'
        }
    },
    mutation,
    actions,
    getters
}